#include "main.h"
/**
 * _puts - show string and puts to stdout
 * @str: string value
 */
void _puts(char *str)
{
	int i;

	for (i = 0; str[c] != '\0'; i++)
	{
	_putchar(str[i]);
	}
	_putchar('\n');
}
