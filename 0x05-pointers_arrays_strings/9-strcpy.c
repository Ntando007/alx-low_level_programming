#include "main.h"
/**
 * _strcpy - copy string to src
 * @dest - value
 * @src - value
 * Return: Success
 */
char *_strcpy(char *dest, char *src)
{
	int i;
	int l;

	for (l = 0; src[l] != '\0'; l++)
	{
	}

	for (l = 0; i <= l; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
