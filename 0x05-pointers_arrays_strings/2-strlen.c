#include "main.h"
#include <stdio.h>
/**
 * int _strlen - returns length of string
 * @s: char string 
 * Return: Always (Success)
 */
int strlen(char *s)
{
	char *s;

	s = i;
	i = _strlen(s);
	printf("%d\n", i);
	return (0);
}
