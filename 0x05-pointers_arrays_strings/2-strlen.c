#include "main.h"
/**
 * int _strlen - returns length of string
 * @s: char string 
 * Return: Always (Success)
 */
int _strlen(char *s)
{
	char c;

	s = i;
	i = _strlen(s);
	printf("%d\n", i);
	return (0);
}
