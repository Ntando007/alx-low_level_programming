#include <main.h>
/**
 * main - compile and check code
 *
 * Return: Always 0
 */
int _abs(int)
{
	if (n < 0)
	{
		return (n * (-1));
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (n);
	}
}
