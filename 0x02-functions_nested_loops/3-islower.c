#include "main.c"
/**
 * main - compile and check code
 *
 * Return: Always 0
 */
int main(void)
{
	int _islower(int c)

	{
		if (c > 'a' && c < 'z'
				{
				return (1);
				}
				

		else 
		{
		return (0);
		}
}
