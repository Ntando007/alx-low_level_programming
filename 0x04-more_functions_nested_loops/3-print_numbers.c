#include "main.h"
/**
 * print_numbers - show numbers from 0 to 9
 * @0 -9: numbers shown
 * Return: Always 0
 */
void print_numbers(void)
{
	int x;
	for (x = 10;x< 20; x++)
	{
		_putchar(x);
	}
	_putchar(10);
}
