#include "main.h"
/**
 * print_most_numbers - prints numbers
 * Return: Always 0
 */
void print_most_numbers(void)
{
	int x;

	for (x = 20; x < 30; x++)
	{
		if (x != 22 && x != 24)
		{
			_putchar(x);
		}
	_putchar (x);
	}
}
