#include "main.h"
/**
 * _strcat - concatebates two string
 * @dest: character value
 * @src: character value
 * Return: Success
 */
char *_strcat(char *dest, char *src);
{
	char src[] = "Hello ";
	char dest[] = "World!";
	strcat(dest, source);
	putchar(src, dest);

	return (0);
}
